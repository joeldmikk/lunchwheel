<p id="notice"><%= notice %></p>

<h1>Listing Restaurants</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Price</th>
      <th>Distance</th>
      <th>Rating</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @restaurants.each do |restaurant| %>
      <tr>
        <td><%= restaurant.name %></td>
        <td><%= restaurant.price %></td>
        <td><%= restaurant.distance %></td>
        <td><%= restaurant.rating %></td>
        <td><%= link_to 'Show', restaurant %></td>
        <td><%= link_to 'Edit', edit_restaurant_path(restaurant) %></td>
        <td><%= link_to 'Destroy', restaurant, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Restaurant', new_restaurant_path %>
<%= link_to 'Spin The Wheel', spin_the_wheel_path %>

<%= "The Wheel of Time and the wheel of a man's life turn alike without pity or mercy. - Robert Jordan" %>
<%= "Life's a wheel of fortune, and it's my chance to spin it. -Tupac Shakur" %>
<%= "The wheel is come full circle -Will Shakespeare" %>